<div class="container d-flex m-3 gap-3 justify-content-center">
  <h1><%= @article.title %></h1>
  <%= link_to "Edit", edit_article_path(@article) %>
  <%= link_to "Delete", article_path(@article), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "link-danger" %>
</div>
<p class="text-center mt-5"><%= @article.content %></p>
